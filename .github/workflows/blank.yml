name: Hello World
on: push:
  branches: 
    - main
    - develop
    - release/*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: echo "Running test cases"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo "Deploy the App"
        
        

